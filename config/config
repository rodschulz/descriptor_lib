# Selector between execution type (descriptor, clustering, metric)
executionType	metric

##############################
# Filter parameters (gaussian, mls or none)
smoothingType	gaussian

# Gaussian smoothing params
gaussianSigma	4
gaussianRadius	0.005

# MLS smoothing params
mlsRadius	0.05

##############################
# Elbow curve generation for clustering
genElbowCurve	false

# Clustering implementation (opencv, custom or stochastic)
implementation	custom

# Clustering options (metric can be euclidean or closestPermutation)
metric		closestPermutation
clusters	4
maxIter		500
stopThres	0.000001
attempts	5
cacheLocation	./cache/

##############################
# Descriptor calculation
targetPoint	4185
patchSize	0.02
normalRadius	-1
bandNumber	8
bandWidth	0.005
bidirectional	false
useProjection	false

# Sequence calculation (stat can be mean or median)
sequenceBin	0.005
sequenceStat	mean

##############################
# Use of synthetic clouds (cube, cylinder or sphere)
useSynthetic	false
synCloudType	cube

##############################
# Metric test case evaluation (metric can be euclidean or closestPermutation)
targetMetric	closestPermutation
permutationSize	1
